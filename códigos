!pip install -U pip setuptools wheel
!pip install -U spacy
!python -m spacy download en_core_web_sm
!pip install spacytextblob
import pkg_resources, imp
imp.reload(pkg_resources)

import spacy
from spacytextblob.spacytextblob import SpacyTextBlob

nlp = spacy.load('en_core_web_sm')  # Carrega o modelo em inglês
nlp.add_pipe('spacytextblob')  # Adiciona o analisador de sentimentos


frase_teste = "This is a wonderful campsite. I loved the serenity and the birds chirping in the morning."
doc = nlp(frase_teste)

sentimento = {
    'texto': frase_teste,
    'pontuacao': doc._.polarity  # Varia de -1 (ruim) a +1 (bom)
}
print(sentimento)

{'texto': 'This is a wonderful campsite...', 'pontuacao': 0.6}

for tweet in tweets:
    doc = nlp(tweet)
    resultado = {
        'tweet': tweet,
        'sentimento': doc._.polarity
    }
    print(resultado)

