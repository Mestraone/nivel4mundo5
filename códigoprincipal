nlp = spacy.load('en_core_web_sm')
nlp.add_pipe('spacytextblob')

tweets = ["Bayer Leverkusen goalkeeper Bernd Leno...", ...]

for item in tweets:
    doc = nlp(item)
    sentiment = {
        'tweet': item,
        'score': doc._.polarity
    }
    print(sentiment)
